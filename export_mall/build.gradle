plugins {
    id "com.android.library"
}

// 引入公共配置
apply from: rootProject.file("common_config.gradle")

dependencies {
    implementation project(":lib_base")
    implementation project(":module_common")
}